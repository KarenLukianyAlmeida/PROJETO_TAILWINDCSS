/** @type {import('tailwindcss').Config} */
const colors = require('tailwindcss/colors')

module.exports = {
  content: [],
  theme: {
    colors: {
      neutral: colors.neutral,
      black: colors.black,
      white: colors.white,
      cyan: colors.cyan,
    },
    extend: {
    },
  },
  plugins: [],
}
